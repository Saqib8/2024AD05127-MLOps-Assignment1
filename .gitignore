# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.ipynb_checkpoints/

# Virtual environments
.venv/
venv/
env/

# MLflow local store (regenerated by training; keep out of git)
mlruns/
mlflow.db

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/

# Keep the trained model & cleaned data tracked (needed for Docker build),
# but ignore large raw intermediates if desired.
# models/ and data/*.csv are intentionally committed.
test-results.xml
